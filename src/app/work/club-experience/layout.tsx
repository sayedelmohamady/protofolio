import type { ReactNode } from "react";

import "@/styles/case-study-club.css";
import "@/styles/case-study-club-experience.css";

export default function ClubExperienceLayout({ children }: { children: ReactNode }) {
  return children;
}
