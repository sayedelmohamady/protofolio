import type { ReactNode } from "react";

import "@/styles/case-study-club.css";
import "@/styles/case-study-club-tie.css";

export default function ClubTieLayout({ children }: { children: ReactNode }) {
  return children;
}
