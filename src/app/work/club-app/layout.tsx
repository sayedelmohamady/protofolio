import type { ReactNode } from "react";

import "@/styles/case-study-club.css";
import "@/styles/case-study-club-app.css";

export default function ClubAppLayout({ children }: { children: ReactNode }) {
  return children;
}
