import type { ReactNode } from "react";

import "@/styles/case-study-club.css";
import "@/styles/case-study-club-app.css";
import "@/styles/case-study-club-dashboard.css";

export default function ClubDashboardLayout({ children }: { children: ReactNode }) {
  return children;
}
