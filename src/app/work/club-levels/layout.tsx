import type { ReactNode } from "react";

import "@/styles/case-study-club.css";
import "@/styles/case-study-club-levels.css";

export default function ClubLevelsLayout({ children }: { children: ReactNode }) {
  return children;
}
