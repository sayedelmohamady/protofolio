import type { ReactNode } from "react";

import "@/styles/case-study-club.css";
import "@/styles/case-study-club-platform.css";

export default function ClubPlatformLayout({ children }: { children: ReactNode }) {
  return children;
}
